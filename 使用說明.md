# 作品集網站使用說明

## 檔案結構
- `index.html`：頁面結構(姓名、ABOUT 內文、CONTACT 資訊在這裡改)
- `projects-data.js`：**作品資料**(換圖、改標題、改分類都在這裡改，最常用)
- `style.css`：樣式(顏色、間距、字型)
- `script.js`：切換頁籤、篩選、放大檢視的功能，通常不用動
- `images/`：圖片資料夾

## 怎麼換 / 新增作品圖片
每個作品現在可以放**一張或多張圖片**：第一張會是 PROJECT 頁面看到的縮圖封面，
點進作品後可以整套上下滑動瀏覽完所有圖片，滑完一個作品會自動接到下一個作品。

1. 建議每個作品在 `images/` 底下自己開一個資料夾(例如 `images/haozhai/`)，把這個作品要用的圖片都放進去。
2. 打開 `projects-data.js`，找到對應作品的 `images: [ ... ]`，把路徑改成新的檔名，例如：
   ```js
   images: [
     "images/haozhai/01-cover.png",
     "images/haozhai/02.jpg",
     "images/haozhai/03.jpg"
   ]
   ```
   陣列第一個是封面縮圖，之後每一行是要放進滑動瀏覽的圖片，要加圖就多加一行、要刪圖就刪掉那一行。
3. 存檔後重新整理網頁即可看到新圖，**不需要動任何其他程式碼**。

同一份檔案裡也可以：
- 改 `title`：作品名稱
- 改 `category`：分類(會自動出現在篩選按鈕，改成一致的名稱就會自動歸類)
- 改 `desc`：沒有填 `about` 時，作品詳情頁的介紹文字會改用這段；也會當成圖片放大檢視預設的說明文字
- 新增/刪除整個作品：複製貼上一整組 `{ ... }`，或整組刪除即可

## 作品詳情頁(點作品卡片後看到的大標題、介紹文字、資訊表、大圖頁面)
每個作品可以再加這幾個欄位，用來填詳情頁上方的資訊表格和介紹文字：
```js
{
  title: "好宅製所 | 品牌設計",
  category: "品牌設計",
  client: "好宅製所",              // 資訊表格：Client
  services: "Brand Identity",      // 資訊表格：Services
  year: "2025",                    // 資訊表格：Year
  location: "Taichung, Taiwan",    // 資訊表格：Location
  about: [                         // ABOUT THE PROJECT 的介紹文字，一段一句
    "第一段文字...",
    "第二段文字...",
    "第三段文字..."
  ],
  desc: "...",
  images: [ ... ]
}
```
`client`／`services`／`year`／`location`／`about` 這幾個都可以不填(不填的話資訊表格會顯示「—」，介紹文字會自動改用 `desc`)。
`about` 是陣列，裡面每一個字串就是一個段落，想加減段落直接增刪就好。

## 怎麼讓某一張圖片有自己專屬的說明文字
`images` 陣列裡，每一項預設是路徑字串（會沿用作品的 `desc`）。
想讓某張圖片顯示不一樣的說明，把那一行改成 `{ src, desc }` 的寫法即可：
```js
images: [
  "images/haozhai/01-cover.jpg",
  { src: "images/haozhai/2.jpg", desc: "這張圖片專屬的說明文字" },
  "images/haozhai/3.jpg"
]
```
只有第 2 張會顯示自己的說明，其他還是沿用作品原本的 `desc`。

## 支援的檔案格式
- 圖片：`.jpg`、`.png`、`.svg`、`.gif`(動圖跟一般圖片一樣用，不用特別設定)
- 影片：`.mp4`(路徑寫法跟圖片完全一樣，系統會自動判斷副檔名，改用可播放的播放器顯示，並附上播放/暫停控制列)

```js
images: [
  "images/xxx/01-cover.jpg",
  "images/xxx/02.gif",
  "images/xxx/03.mp4"
]
```
⚠️ 影片檔通常比圖片大很多，建議上線前先壓縮/降低解析度(可以用免費工具如 HandBrake),不然網頁會很慢。

⚠️ 注意：目前放的原始圖檔案很大(部分好幾 MB)，正式上線前建議壓縮/縮小尺寸(網頁用圖通常抓 200KB～1MB 以內即可)，不然網頁開啟會很慢。需要的話我可以幫你批次壓縮。

## 怎麼改 ABOUT / CONTACT 文字
直接打開 `index.html`，找到 `<section id="about">` 或 `<section id="contact">`，
裡面的文字都可以直接修改(例如自我介紹、學歷、經歷、Email、社群連結)。

## 怎麼改大頭照
把新照片放進 `images/` 資料夾，然後在 `index.html` 裡找到：
```html
<img src="images/placeholder.svg" alt="Kaiting Chuang">
```
把 `images/placeholder.svg` 改成新照片的檔名。

## 如何預覽網站
用瀏覽器直接打開 `index.html` 即可預覽，不需要架設伺服器。

## 如何正式上線
建議使用 GitHub Pages、Netlify 或 Vercel，把這個資料夾整包上傳即可免費架站。
若需要，我可以協助你完成部署。
